interface:
  display_name: "模块化合规小说连载"
  short_description: "按单部小说隔离记忆，编排续写、润色、审查与长期维护"
  default_prompt: "Use $write-compliant-fiction-serial to route my Chinese serial-fiction task through only the needed modular components, resolving one isolated novel project before reading or persisting memory."
